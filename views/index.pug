extends ./layout.pug

include ./mixins.pug
block title
  title Articles and Products
block body
  h1#header.headerClass Products


  ul.productList
    +productsList(products)

  - var url = 'http://localhost:3000/products/new'
  a(href=url) Create a new product
  br
